{
  "name": "ioc-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Type-driven, interface-first dependency injection for TypeScript (monorepo root; individual packages publish to npm under @fnioc)",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "lint": "moon run :lint",
    "test": "moon run :test",
    "build": "moon run :build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "dprint": "^0.54.0",
    "eslint": "^10.4.1",
    "rollup": "^4.61.0",
    "rollup-plugin-dts": "^6.4.1",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.60.1"
  }
}
