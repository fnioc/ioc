{
  "name": "@fnioc-examples/shared",
  "private": true,
  "version": "0.0.0",
  "description": "Shared, source-only contracts + service classes wired by both @fnioc/di examples. No build step — main/types point at the TypeScript source, which each example compiles in via a relative import.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "moon run examples-shared:lint"
  },
  "devDependencies": {
    "@fnioc/di": "workspace:*"
  }
}
